/**
 * 旧版「工具调度协议」曾禁止角色起调。现已改为：
 * 点歌/语音/生图/找图 → 角色标记优先，调度员兜底；Keep → 调度员主责。
 * 本常量不再注入；仅供对照。
 */
export const TOOL_DISPATCH_PROTOCOL = `【工具调度 · 已弃用管嘴版】
点歌 / 语音 / 生图 / 找图由角色用末尾标记起调；调度员仅漏调兜底。Keep 由调度员与后端查询。`;
